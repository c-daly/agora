interface_contracts:
  module: agora.adapters.sec_ftd_adapter
  functions:
    - name: fetch_ftd_data
      signature: "(*, symbol: str | None = None, start_date: date | None = None, end_date: date | None = None) -> list[ShortData]"

do_not_do:
  - "Do not hardcode file URLs — derive from date parameters"
  - "Do not fail on malformed rows — skip and continue"

escalate_if: []

known_findings: []
